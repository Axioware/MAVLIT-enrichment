weight for the initial brand score table to rank the brand for apollo run

weight scoring should be in classes like (for meta ads frequently ads, recent ads, with no end limit etc) then save in meta ad score column in the new table 

also add the user payment method can be use 2 free

signup and signin page only oauth signup using google

have to check for the oauth code manually 
check all the apis made 
check the frontend